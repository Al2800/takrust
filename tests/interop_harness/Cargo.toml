[package]
name = "interop-harness-tests"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
rustak-bridge = { path = "../../crates/rustak-bridge" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"

[workspace]
